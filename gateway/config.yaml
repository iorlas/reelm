binds:
- port: 3000
  listeners:
  - routes:
    - backends:
      - mcp:
          targets:
          - name: reelm_torrents
            mcp:
              host: http://reelm-transmission:8000/mcp/
          - name: reelm_search
            mcp:
              host: http://reelm-jackett:8000/mcp/
          - name: reelm_storage
            mcp:
              host: http://reelm-storage:8000/mcp/
      matches:
      - path:
          exact: /mcp
      - path:
          exact: /.well-known/oauth-protected-resource/mcp
      policies:
        cors:
          allowOrigins: ["*"]
          allowHeaders: ["*"]
          exposeHeaders: ["Mcp-Session-Id"]
        mcpAuthentication:
          mode: strict
          issuer: https://pioneering-friendship-09-staging.authkit.app/
          jwks:
            url: https://pioneering-friendship-09-staging.authkit.app/.well-known/jwks.json
          resourceMetadata:
            resource: https://reelm.shen.iorlas.net/mcp
            scopesSupported:
            - openid
            - profile
            bearerMethodsSupported:
            - header
